---
import { getCollection } from 'astro:content';
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import MedicalDisclaimer from '../components/MedicalDisclaimer.astro';
import FormattedDate from '../components/FormattedDate.astro';
import { sortPostsDescending } from '../utils/postUtils';

const rawPosts = await getCollection('blog', ({ data }) => !data.draft);
const posts = sortPostsDescending(rawPosts);
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead 
			title="The Decoding the Burning Mouth Syndrome Podcast | Denis Ethier" 
			description="The Decoding the Burning Mouth Syndrome Podcast. Advancing scientific research on Burning Mouth Syndrome through radical data transparency and patient-clinician conversations." 
		/>
	</head>
	<body>
		<Header />
		<main class="podcast-page-container">
			<div class="podcast-disclaimer-wrap">
				<MedicalDisclaimer />
			</div>

			<!-- Hero Section -->
			<header class="podcast-hero">
				<h1 class="podcast-title">The Decoding the Burning Mouth Syndrome Podcast</h1>
				<p class="podcast-lead-subhead">
					Advancing scientific research on Burning Mouth Syndrome through radical data transparency, patient-clinician conversations, and forensic root-cause&nbsp;investigation.
				</p>
			</header>

			<!-- Episodes & Casebook Archive Section (Now at Top) -->
			<section class="episodes-section" id="episodes">
				<div class="episodes-center-head">
					<h2 class="episodes-heading">Podcast Episodes &amp; Research Notes</h2>
					<p class="episodes-subheading">Listen, read, and explore our full series of deep dives, self-experiments, and clinical case studies.</p>
				</div>

				<div class="episodes-grid">
					{
						posts.filter(p => p.id === 'how-i-solved-my-burning-mouth-syndrome').map((post) => {
							const imageSrc = typeof post.data.heroImage === 'string' ? post.data.heroImage : post.data.heroImage?.src;
							return (
								<article class="episode-card">
									<a href={`/blog/${post.id}/`} class="episode-card-link">
										<div class="episode-img-wrapper">
											<img 
												src={imageSrc || '/how-i-solved-my-bms-youtube-thumbnail.jpg'} 
												alt={post.data.title} 
												class="episode-img" 
												loading="lazy"
											/>
										</div>
										<div class="episode-body">
											<div class="episode-meta">
												<span class="episode-number-badge">Episode 1</span>
												<span class="meta-dot">•</span>
												<span class="meta-date"><FormattedDate date={post.data.pubDate} /></span>
											</div>
											<h3 class="episode-title" set:html={post.data.title} />
											<p class="episode-desc">{post.data.description}</p>
											<div class="episode-footer">
												<span class="listen-link">Listen &amp; Read Episode &rarr;</span>
											</div>
										</div>
									</a>
								</article>
							);
						})
					}
				</div>
			</section>

			<!-- Notify of New Episode Box (Positioned Below Episode List) -->
			<section class="notify-wrap-section">
				<div class="podcast-notify-box" id="notify">
					<div class="notify-icon-badge" aria-hidden="true">🎙️</div>
					<h2 class="notify-title">Get Notified of New Episodes</h2>
					<p class="notify-subtext">
						Drop your email below to receive a direct link as new podcast interviews and case studies go live. No spam,&nbsp;ever.
					</p>

					<form 
						id="podcast-notify-form" 
						action="https://formsubmit.co/denis@denisethier.com" 
						method="POST" 
						class="notify-form"
					>
						<input type="hidden" name="_subject" value="[BMS Podcast] New Episode Subscriber" />
						<input type="hidden" name="_captcha" value="false" />
						<input type="hidden" name="_template" value="table" />
						<input type="hidden" name="form_type" value="Podcast Episode Notification" />

						<div class="notify-input-row">
							<input 
								type="email" 
								name="email" 
								placeholder="Enter your email address..." 
								required 
								class="notify-email-input"
								aria-label="Email address for podcast notification"
							/>
							<button type="submit" id="notify-submit-btn" class="notify-submit-btn">
								<span class="notify-btn-text">Notify Me &rarr;</span>
								<span class="notify-btn-spinner hidden" aria-hidden="true">Subscribing...</span>
							</button>
						</div>
					</form>

					<div id="notify-success-box" class="notify-success-msg hidden" role="alert">
						<span class="notify-check-icon">✓</span>
						<span>You are on the list! I will email you as soon as the next episode&nbsp;airs.</span>
					</div>
				</div>
			</section>

			<!-- Guest Categories Grid -->
			<section class="guest-categories-section">
				<div class="section-center-head">
					<h2 class="section-title">Who We Interview on the Podcast</h2>
					<p class="section-subtitle">We bring four key perspectives together to advance scientific understanding from every angle.</p>
				</div>

				<div class="guest-categories-grid" aria-label="Who we are inviting">
					<div class="category-card">
						<div class="category-icon-wrapper" aria-hidden="true">
							<span class="category-emoji">🗣️</span>
						</div>
						<h3 class="category-title">Patients</h3>
						<p class="category-description">
							Share your timeline, onset triggers, pain fluctuations, what failed, and what strategies brought you&nbsp;relief.
						</p>
					</div>

					<div class="category-card">
						<div class="category-icon-wrapper" aria-hidden="true">
							<span class="category-emoji">🦷</span>
						</div>
						<h3 class="category-title">Dentists &amp; Specialists</h3>
						<p class="category-description">
							Discuss cervical margins, exposed tubules, hidden crown decay, galvanism, and periodontal biofilm&nbsp;pathways.
						</p>
					</div>

					<div class="category-card">
						<div class="category-icon-wrapper" aria-hidden="true">
							<span class="category-emoji">🩺</span>
						</div>
						<h3 class="category-title">Medical Doctors</h3>
						<p class="category-description">
							Explore small-fiber neuropathy, trigeminal sensory pathways, autonomic dysfunction, and gaps in modern clinical&nbsp;training.
						</p>
					</div>

					<div class="category-card">
						<div class="category-icon-wrapper" aria-hidden="true">
							<span class="category-emoji">🔬</span>
						</div>
						<h3 class="category-title">Researchers</h3>
						<p class="category-description">
							Highlight emerging science on acid-sensing ion channels (ASIC/TRPV1), salivary chemistry, and dietary elimination&nbsp;trials.
						</p>
					</div>
				</div>
			</section>

			<!-- Apply to Be a Guest Section -->
			<section class="podcast-form-card" id="apply">
				<div class="form-header">
					<h2 class="form-heading">Apply to Be a Guest on the Podcast</h2>
					<p class="form-subtext">
						Whether you are a patient living with BMS or a clinician with insights to share, Denis reviews every submission personally.
					</p>
					<div class="guest-guide-callout-grid">
						<div class="guest-guide-callout-item">
							<span class="guide-callout-label">For Patients:</span>
							<a href="/podcast/guest-guide" class="inline-guide-link"><strong>BMS Sufferer Guest Guide &amp; Question List &rarr;</strong></a>
						</div>
						<div class="guest-guide-callout-item">
							<span class="guide-callout-label">For Clinicians &amp; Researchers:</span>
							<a href="/podcast/professional-guide" class="inline-guide-link"><strong>Clinician &amp; Researcher Guide &rarr;</strong></a>
						</div>
					</div>
				</div>

				<form 
					id="podcast-guest-form" 
					action="https://formsubmit.co/denis@denisethier.com" 
					method="POST" 
					class="guest-form"
				>
					<input type="hidden" name="_subject" value="[BMS Podcast Guest Application] Decoding The Burn" />
					<input type="hidden" name="_captcha" value="false" />
					<input type="hidden" name="_template" value="table" />

					<!-- Guest Role Selection -->
					<div class="form-group">
						<label class="form-label">I am applying as a:</label>
						<div class="role-selector-grid">
							<label class="role-pill">
								<input type="radio" name="guest_role" value="Patient" checked />
								<span class="role-label-content">🗣️ Patient</span>
							</label>

							<label class="role-pill">
								<input type="radio" name="guest_role" value="Dentist" />
								<span class="role-label-content">🦷 Dentist</span>
							</label>

							<label class="role-pill">
								<input type="radio" name="guest_role" value="Medical Doctor" />
								<span class="role-label-content">🩺 Medical Doctor</span>
							</label>

							<label class="role-pill">
								<input type="radio" name="guest_role" value="Scientific Researcher" />
								<span class="role-label-content">🔬 Researcher</span>
							</label>

							<label class="role-pill">
								<input type="radio" name="guest_role" value="Other Specialist" />
								<span class="role-label-content">💡 Other Specialist</span>
							</label>
						</div>
					</div>

					<div class="form-row">
						<div class="form-group flex-1">
							<label for="guest-name" class="form-label">Your Name</label>
							<input 
								type="text" 
								id="guest-name" 
								name="name" 
								placeholder="e.g., Denis Ethier" 
								required 
								class="text-input" 
							/>
						</div>

						<div class="form-group flex-1">
							<label for="guest-email" class="form-label">Your Email Address</label>
							<input 
								type="email" 
								id="guest-email" 
								name="email" 
								placeholder="e.g., name@example.com" 
								required 
								class="text-input" 
							/>
						</div>
					</div>

					<div class="form-group">
						<label for="guest-story" class="form-label">
							What is your background, and what would you like to share?
						</label>
						<textarea 
							id="guest-story" 
							name="background_and_story" 
							rows="5" 
							placeholder="Tell me about your clinical background, research focus, or personal journey with Burning Mouth Syndrome. What insights would you bring to fellow listeners?" 
							required 
							class="text-area-input"
						></textarea>
					</div>

					<div class="form-submit-row">
						<button type="submit" id="guest-submit-btn" class="btn btn-primary submit-btn">
							<span class="btn-text">Send Guest Application &rarr;</span>
							<span class="btn-spinner hidden" aria-hidden="true">Submitting...</span>
						</button>
					</div>

					<p class="privacy-notice">
						Your email and information remain strictly confidential. I will only use your details to coordinate podcast appearances.
					</p>
				</form>

				<!-- Success State Card -->
				<div id="form-success-box" class="form-success-card hidden" role="alert">
					<div class="success-icon-circle">✓</div>
					<h3 class="success-title">Thank you for your application!</h3>
					<p class="success-message">
						Your details have been sent directly to Denis Ethier at <strong class="user-email-highlight">denis@denisethier.com</strong>.
					</p>
					<p class="success-followup">
						I review every application personally and will reach out to you by email to coordinate scheduling.
					</p>
					<button type="button" id="reset-form-btn" class="btn btn-outline success-reset-btn">
						Submit Another Note
					</button>
				</div>

				<!-- Error Banner -->
				<div id="form-error-banner" class="form-error-banner hidden" role="alert">
					An error occurred while submitting. Please try again, or email Denis directly at 
					<a href="mailto:denis@denisethier.com">denis@denisethier.com</a>.
				</div>
			</section>
		</main>
		<Footer />

		<script>
			const setupGuestForm = () => {
				const form = document.getElementById('podcast-guest-form') as HTMLFormElement | null;
				const submitBtn = document.getElementById('guest-submit-btn') as HTMLButtonElement | null;
				const btnText = submitBtn?.querySelector('.btn-text');
				const btnSpinner = submitBtn?.querySelector('.btn-spinner');
				const successBox = document.getElementById('form-success-box');
				const errorBanner = document.getElementById('form-error-banner');
				const resetBtn = document.getElementById('reset-form-btn');

				if (!form || !submitBtn) return;

				form.addEventListener('submit', async (e) => {
					e.preventDefault();
					errorBanner?.classList.add('hidden');

					submitBtn.disabled = true;
					btnText?.classList.add('hidden');
					btnSpinner?.classList.remove('hidden');

					const formData = new FormData(form);

					try {
						const response = await fetch(form.action, {
							method: 'POST',
							body: formData,
							headers: {
								'Accept': 'application/json'
							}
						});

						if (response.ok) {
							form.classList.add('hidden');
							successBox?.classList.remove('hidden');
							successBox?.scrollIntoView({ behavior: 'smooth', block: 'nearest' });
						} else {
							form.submit();
						}
					} catch (err) {
						form.submit();
					} finally {
						submitBtn.disabled = false;
						btnText?.classList.remove('hidden');
						btnSpinner?.classList.add('hidden');
					}
				});

				resetBtn?.addEventListener('click', () => {
					form.reset();
					successBox?.classList.add('hidden');
					form.classList.remove('hidden');
				});

				// Top Notify Form Handler
				const notifyForm = document.getElementById('podcast-notify-form') as HTMLFormElement | null;
				const notifySubmitBtn = document.getElementById('notify-submit-btn') as HTMLButtonElement | null;
				const notifyBtnText = notifySubmitBtn?.querySelector('.notify-btn-text');
				const notifyBtnSpinner = notifySubmitBtn?.querySelector('.notify-btn-spinner');
				const notifySuccessBox = document.getElementById('notify-success-box');

				if (notifyForm && notifySubmitBtn) {
					notifyForm.addEventListener('submit', async (e) => {
						e.preventDefault();

						notifySubmitBtn.disabled = true;
						notifyBtnText?.classList.add('hidden');
						notifyBtnSpinner?.classList.remove('hidden');

						const formData = new FormData(notifyForm);

						try {
							const response = await fetch(notifyForm.action, {
								method: 'POST',
								body: formData,
								headers: {
									'Accept': 'application/json'
								}
							});

							if (response.ok) {
								notifyForm.classList.add('hidden');
								notifySuccessBox?.classList.remove('hidden');
							} else {
								notifyForm.submit();
							}
						} catch (err) {
							notifyForm.submit();
						} finally {
							notifySubmitBtn.disabled = false;
							notifyBtnText?.classList.remove('hidden');
							notifyBtnSpinner?.classList.add('hidden');
						}
					});
				}
			};

			if (document.readyState === 'loading') {
				document.addEventListener('DOMContentLoaded', setupGuestForm);
			} else {
				setupGuestForm();
			}
		</script>

		<style>
			.podcast-page-container {
				max-width: 1080px;
				margin: 1.5rem auto 5rem;
				padding: 0 1.5rem;
			}

			.podcast-disclaimer-wrap {
				margin-bottom: 2rem;
			}

			.podcast-hero {
				text-align: center;
				margin-bottom: 4rem;
				max-width: 860px;
				margin-left: auto;
				margin-right: auto;
			}

			.podcast-title {
				font-size: clamp(2.25rem, 5vw, 3.25rem);
				font-weight: 800;
				color: #000000;
				letter-spacing: -0.03em;
				line-height: 1.15;
				margin-bottom: 1.25rem;
				text-wrap: balance;
			}

			.podcast-lead-subhead {
				font-size: clamp(1.05rem, 2.5vw, 1.25rem);
				color: #334155;
				line-height: 1.65;
				max-width: 760px;
				margin: 0 auto;
				text-wrap: balance;
			}

			.podcast-quick-actions {
				display: flex;
				justify-content: center;
				margin-top: 2rem;
			}

			.apply-btn-prominent {
				padding: 0.95rem 2rem;
				font-weight: 800;
				font-size: 1.05rem;
				border-radius: 14px;
				text-decoration: none;
				box-shadow: 0 6px 20px -3px rgba(190, 18, 60, 0.35);
			}

			/* Top Notify Box in Brand Red */
			.podcast-notify-box {
				background: linear-gradient(135deg, #be123c 0%, #9f1239 100%);
				border: 1px solid #e11d48;
				border-radius: 20px;
				padding: 2.25rem 2rem;
				max-width: 640px;
				margin: 2.5rem auto 0;
				text-align: center;
				color: #ffffff;
				box-shadow: 0 14px 36px -6px rgba(190, 18, 60, 0.35);
			}

			.notify-icon-badge {
				font-size: 2rem;
				line-height: 1;
				margin-bottom: 0.75rem;
			}

			.notify-title {
				font-size: 1.6rem;
				font-weight: 800;
				color: #ffffff;
				letter-spacing: -0.02em;
				margin-bottom: 0.5rem;
				text-wrap: balance;
			}

			.notify-subtext {
				font-size: 0.98rem;
				color: #ffe4e6;
				line-height: 1.55;
				margin-bottom: 1.35rem;
				text-wrap: pretty;
			}

			.notify-input-row {
				display: flex;
				gap: 0.75rem;
			}

			@media (max-width: 540px) {
				.notify-input-row {
					flex-direction: column;
				}
			}

			.notify-email-input {
				flex: 1;
				padding: 0.9rem 1.15rem;
				border-radius: 12px;
				border: 1.5px solid rgba(255, 255, 255, 0.3);
				background: #ffffff;
				font-family: inherit;
				font-size: 0.95rem;
				color: #0f172a;
				transition: border-color 0.2s ease, box-shadow 0.2s ease;
			}

			.notify-email-input:focus {
				outline: none;
				border-color: #ffffff;
				box-shadow: 0 0 0 3px rgba(255, 255, 255, 0.3);
			}

			.notify-submit-btn {
				padding: 0.9rem 1.6rem;
				font-size: 0.98rem;
				font-weight: 800;
				border-radius: 12px;
				white-space: nowrap;
				background: #0f172a;
				color: #ffffff;
				border: none;
				cursor: pointer;
				transition: background 0.2s ease, transform 0.2s ease;
			}

			.notify-submit-btn:hover {
				background: #000000;
				transform: translateY(-1px);
			}

			.notify-success-msg {
				margin-top: 1rem;
				padding: 0.85rem 1rem;
				background: rgba(255, 255, 255, 0.15);
				border: 1px solid rgba(255, 255, 255, 0.3);
				border-radius: 10px;
				color: #ffffff;
				font-size: 0.95rem;
				font-weight: 700;
				display: flex;
				align-items: center;
				justify-content: center;
				gap: 0.5rem;
			}

			/* Episodes Section */
			.episodes-section {
				margin-bottom: 5rem;
			}

			.episodes-center-head {
				text-align: center;
				max-width: 700px;
				margin: 0 auto 2.5rem auto;
				border-bottom: 2px solid #e2e8f0;
				padding-bottom: 1.5rem;
			}

			.episodes-heading {
				font-size: clamp(1.75rem, 3.5vw, 2.25rem);
				font-weight: 800;
				color: #0f172a;
				letter-spacing: -0.025em;
				margin: 0 0 0.5rem 0;
				text-wrap: balance;
			}

			.episodes-subheading {
				font-size: 1.05rem;
				color: #64748b;
				margin: 0;
				text-wrap: balance;
			}

			/* Max 2 side-by-side, centered when odd or single */
			.episodes-grid {
				display: grid;
				grid-template-columns: repeat(auto-fit, minmax(340px, 480px));
				justify-content: center;
				gap: 2rem;
			}

			.episode-card {
				background: #ffffff;
				border: 1px solid #e2e8f0;
				border-radius: 16px;
				overflow: hidden;
				box-shadow: 0 8px 24px -4px rgba(15, 23, 42, 0.08);
				transition: transform 0.2s ease, box-shadow 0.2s ease, border-color 0.2s ease;
				display: flex;
				flex-direction: column;
				width: 100%;
				max-width: 480px;
				margin: 0 auto;
			}

			.episode-card:hover {
				transform: translateY(-3px);
				box-shadow: 0 14px 30px -4px rgba(15, 23, 42, 0.12);
				border-color: #cbd5e1;
			}

			.episode-card-link {
				color: inherit;
				text-decoration: none;
				display: flex;
				flex-direction: column;
				height: 100%;
			}

			.episode-img-wrapper {
				width: 100%;
				aspect-ratio: 16 / 9;
				overflow: hidden;
				background: #0f172a;
				border-bottom: 1px solid #f1f5f9;
			}

			.episode-img {
				width: 100%;
				height: 100%;
				object-fit: cover;
				display: block;
				transition: transform 0.25s ease;
			}

			.episode-card:hover .episode-img {
				transform: scale(1.02);
			}

			.episode-body {
				padding: 1.5rem 1.65rem 1.65rem;
				display: flex;
				flex-direction: column;
				flex: 1;
			}

			.episode-meta {
				display: flex;
				align-items: center;
				gap: 0.5rem;
				font-size: 0.88rem;
				color: #475569;
				margin-bottom: 0.65rem;
			}

			.episode-number-badge {
				font-weight: 700;
				color: #be123c;
			}

			.meta-dot {
				color: #be123c;
				font-weight: 800;
			}

			.meta-category {
				font-weight: 600;
				color: #be123c;
			}

			.episode-title {
				font-size: 1.28rem;
				font-weight: 800;
				line-height: 1.35;
				color: #0f172a;
				margin: 0 0 0.65rem 0;
				letter-spacing: -0.015em;
			}

			.episode-desc {
				font-size: 0.94rem;
				line-height: 1.6;
				color: #475569;
				margin: 0 0 1.25rem 0;
				display: -webkit-box;
				-webkit-line-clamp: 3;
				-webkit-box-orient: vertical;
				overflow: hidden;
				flex: 1;
			}

			.episode-footer {
				margin-top: auto;
				padding-top: 0.85rem;
				border-top: 1px solid #f1f5f9;
			}

			.listen-link {
				font-size: 0.92rem;
				font-weight: 700;
				color: #be123c;
			}

			/* Guest Categories - 2x2 on vertical/portrait and desktop */
			.guest-categories-section {
				margin-top: 4.5rem;
				margin-bottom: 5rem;
				background: #f1f5f9;
				border: 1px solid #e2e8f0;
				border-radius: 24px;
				padding: 3.5rem 2.25rem;
			}

			.section-center-head {
				text-align: center;
				max-width: 650px;
				margin: 0 auto 2.5rem auto;
			}

			.section-title {
				font-size: clamp(1.65rem, 3vw, 2.1rem);
				font-weight: 800;
				color: #0f172a;
				margin: 0 0 0.5rem 0;
				letter-spacing: -0.02em;
				text-wrap: balance;
			}

			.section-subtitle {
				font-size: 1.05rem;
				color: #475569;
				margin: 0;
				line-height: 1.6;
				text-wrap: balance;
			}

			.guest-categories-grid {
				display: grid;
				grid-template-columns: repeat(2, 1fr);
				gap: 1.5rem;
			}

			@media (max-width: 600px) {
				.guest-categories-grid {
					grid-template-columns: 1fr;
				}
			}

			.category-card {
				background: #ffffff;
				border: 1px solid #e2e8f0;
				border-radius: 16px;
				padding: 2rem 1.85rem;
				display: flex;
				flex-direction: column;
				box-shadow: 0 4px 14px -3px rgba(15, 23, 42, 0.04);
			}

			.category-icon-wrapper {
				font-size: 3.5rem;
				line-height: 1.1;
				margin-bottom: 1rem;
				user-select: none;
			}

			.category-emoji {
				display: inline-block;
				transform: scale(1);
			}

			.category-title {
				font-size: 1.25rem;
				font-weight: 800;
				color: #0f172a;
				margin: 0 0 0.5rem 0;
			}

			.category-description {
				font-size: 0.96rem;
				line-height: 1.6;
				color: #475569;
				margin: 0;
				flex: 1;
				text-wrap: pretty;
			}

			/* Inverted Striking Guest Application Card */
			.podcast-form-card {
				background: linear-gradient(135deg, #090d16 0%, #15102a 100%);
				border: 1px solid rgba(168, 85, 247, 0.3);
				border-radius: 24px;
				padding: 3.25rem 2.75rem;
				max-width: 860px;
				margin: 0 auto;
				color: #ffffff;
				box-shadow: 0 20px 50px -10px rgba(0, 0, 0, 0.5);
			}

			@media (max-width: 640px) {
				.podcast-form-card {
					padding: 2rem 1.5rem;
				}
			}

			.form-header {
				text-align: center;
				margin-bottom: 2.5rem;
			}

			.form-heading {
				font-size: clamp(1.75rem, 3.5vw, 2.35rem);
				font-weight: 800;
				color: #ffffff;
				letter-spacing: -0.025em;
				margin-bottom: 0.75rem;
				text-wrap: balance;
			}

			.form-subtext {
				font-size: 1.05rem;
				color: #cbd5e1;
				max-width: 620px;
				margin: 0 auto 1.5rem auto;
				line-height: 1.6;
				text-wrap: balance;
			}

			.guest-guide-callout-grid {
				display: flex;
				align-items: center;
				justify-content: center;
				gap: 1rem;
				flex-wrap: wrap;
				margin-top: 1rem;
			}

			.guest-guide-callout-item {
				background: rgba(255, 255, 255, 0.08);
				border: 1px solid rgba(255, 255, 255, 0.2);
				padding: 0.65rem 1.15rem;
				border-radius: 12px;
				font-size: 0.9rem;
				color: #e2e8f0;
				display: flex;
				align-items: center;
				gap: 0.5rem;
				flex-wrap: wrap;
			}

			.guide-callout-label {
				color: #cbd5e1;
				font-size: 0.85rem;
			}

			.inline-guide-link {
				color: #f43f5e;
				text-decoration: none;
			}

			.inline-guide-link:hover {
				text-decoration: underline;
			}

			.guest-form {
				display: flex;
				flex-direction: column;
				gap: 1.5rem;
			}

			.form-group {
				display: flex;
				flex-direction: column;
				gap: 0.5rem;
			}

			.form-row {
				display: flex;
				gap: 1.25rem;
			}

			@media (max-width: 640px) {
				.form-row {
					flex-direction: column;
				}
			}

			.flex-1 {
				flex: 1;
			}

			.form-label {
				font-size: 0.95rem;
				font-weight: 700;
				color: #f1f5f9;
			}

			.role-selector-grid {
				display: grid;
				grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
				gap: 0.65rem;
			}

			.role-pill {
				cursor: pointer;
				position: relative;
			}

			.role-pill input {
				position: absolute;
				opacity: 0;
				width: 0;
				height: 0;
			}

			.role-label-content {
				display: flex;
				align-items: center;
				justify-content: center;
				padding: 0.75rem 0.85rem;
				background: rgba(255, 255, 255, 0.06);
				border: 1.5px solid rgba(255, 255, 255, 0.15);
				border-radius: 12px;
				font-size: 0.9rem;
				font-weight: 600;
				color: #e2e8f0;
				text-align: center;
				transition: all 0.16s ease;
			}

			.role-pill input:checked + .role-label-content {
				background: #be123c;
				border-color: #f43f5e;
				color: #ffffff;
				font-weight: 700;
				box-shadow: 0 0 0 1px #f43f5e;
			}

			.text-input, .text-area-input {
				padding: 0.95rem 1.2rem;
				border-radius: 12px;
				border: 1.5px solid rgba(255, 255, 255, 0.2);
				background: rgba(15, 23, 42, 0.6);
				font-family: inherit;
				font-size: 0.98rem;
				color: #ffffff;
				transition: border-color 0.2s ease, box-shadow 0.2s ease;
			}

			.text-input:focus, .text-area-input:focus {
				outline: none;
				border-color: #f43f5e;
				box-shadow: 0 0 0 3px rgba(244, 63, 94, 0.25);
			}

			.text-input::placeholder, .text-area-input::placeholder {
				color: #64748b;
			}

			.form-submit-row {
				margin-top: 0.5rem;
			}

			.submit-btn {
				width: 100%;
				padding: 1.1rem;
				font-size: 1.1rem;
				font-weight: 800;
				border-radius: 14px;
				background: #be123c;
				color: #ffffff;
				border: none;
				cursor: pointer;
				box-shadow: 0 6px 20px -3px rgba(190, 18, 60, 0.4);
				transition: background 0.2s ease, transform 0.2s ease;
			}

			.submit-btn:hover {
				background: #9f1239;
				transform: translateY(-1px);
			}

			.privacy-notice {
				font-size: 0.85rem;
				color: #94a3b8;
				text-align: center;
				margin: 0;
			}

			.form-success-card {
				text-align: center;
				padding: 2.5rem 1.5rem;
			}

			.success-icon-circle {
				width: 56px;
				height: 56px;
				background: #dcfce7;
				color: #16a34a;
				border-radius: 50%;
				font-size: 1.75rem;
				display: flex;
				align-items: center;
				justify-content: center;
				margin: 0 auto 1.25rem auto;
				font-weight: 800;
			}

			.success-title {
				font-size: 1.6rem;
				font-weight: 800;
				color: #0f172a;
				margin-bottom: 0.65rem;
			}

			.success-message {
				font-size: 1.05rem;
				color: #334155;
				margin-bottom: 0.5rem;
			}

			.success-followup {
				font-size: 0.95rem;
				color: #64748b;
				margin-bottom: 1.5rem;
			}

			.form-error-banner {
				margin-top: 1rem;
				padding: 1rem;
				background: #fef2f2;
				border: 1px solid #fca5a5;
				border-radius: 10px;
				color: #991b1b;
				font-size: 0.92rem;
				text-align: center;
			}

			.hidden {
				display: none !important;
			}
		</style>
	</body>
</html>
