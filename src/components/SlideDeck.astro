---
import TongueLogo from './TongueLogo.astro';
import { bmsSlides, type SlideItem } from '../data/slidesData';

interface Props {
	slides?: SlideItem[];
	isStandalone?: boolean;
}

const { slides = bmsSlides, isStandalone = false } = Astro.props;
---

<div class={`deck-wrapper ${isStandalone ? 'standalone-mode' : 'embedded-mode'}`} id="bms-slide-deck">
	<!-- Top Toolbar (for embedded view) -->
	{!isStandalone && (
		<div class="deck-top-bar">
			<div class="deck-title-group">
				<span class="deck-live-badge">INTERACTIVE PRESENTATION</span>
				<span class="deck-meta">8 Slides • Short 5-Word Cues • Fullscreen Ready</span>
			</div>
			<div class="deck-actions">
				<a href="/viewer/public" target="_blank" class="deck-btn deck-btn-outline" title="Open Clean 16:9 Public Screen for OBS">
					<svg viewBox="0 0 24 24" width="15" height="15" stroke="currentColor" stroke-width="2" fill="none"><rect x="2" y="3" width="20" height="14" rx="2" ry="2"></rect><line x1="8" y1="21" x2="16" y2="21"></line><line x1="12" y1="17" x2="12" y2="21"></line></svg>
					<span>Public Viewer</span>
				</a>
				<a href="/viewer/private" target="_blank" class="deck-btn deck-btn-outline" title="Open Dennis's Private Presenter Cockpit (Teleprompter)">
					<svg viewBox="0 0 24 24" width="15" height="15" stroke="currentColor" stroke-width="2" fill="none"><path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"></path></svg>
					<span>Private Cockpit</span>
				</a>
				<button type="button" class="deck-btn deck-btn-primary toggle-fullscreen-btn" title="Toggle Fullscreen Presenter Mode (F)">
					<svg viewBox="0 0 24 24" width="15" height="15" stroke="currentColor" stroke-width="2" fill="none"><path d="M8 3H5a2 2 0 0 0-2 2v3m18 0V5a2 2 0 0 0-2-2h-3m0 18h3a2 2 0 0 0 2-2v-3M3 16v3a2 2 0 0 0 2 2h3"></path></svg>
					<span>Fullscreen [F]</span>
				</button>
			</div>
		</div>
	)}

	<!-- Main 16:9 Presentation Stage -->
	<div class="slide-stage-container" id="slide-stage-container" tabindex="0">
		<div class="slide-stage" id="slide-stage">
			<!-- Slide Header -->
			<div class="slide-stage-header">
				<div class="slide-badge-box">
					<span class="slide-counter-badge" id="slide-counter-badge">SLIDE 01 / 08</span>
					<span class="slide-topic-badge" id="slide-topic-badge">Title</span>
				</div>
				<div class="slide-brand">
					<div class="brand-logo-mini">
						<TongueLogo />
					</div>
					<span class="brand-text">Decoding The Burn</span>
				</div>
			</div>

			<!-- Slide Dynamic Content -->
			<div class="slide-body">
				<h2 class="slide-title" id="slide-title">How I Solved My BMS</h2>
				
				<ul class="slide-bullets" id="slide-bullets">
					<!-- Generated dynamically by script -->
				</ul>
			</div>

			<!-- Slide Footer & Controls -->
			<div class="slide-stage-footer">
				<div class="footer-left">
					<span class="point-counter" id="point-counter">Point 1 of 5</span>
					<span class="footer-hint">Press <kbd>Spacebar</kbd> to advance</span>
				</div>
				<div class="footer-center">
					<button type="button" class="nav-control-btn prev-point-btn" id="prev-point-btn" title="Previous Point (Shift+Space or ←)">
						<svg viewBox="0 0 24 24" width="18" height="18" stroke="currentColor" stroke-width="2.5" fill="none"><polyline points="15 18 9 12 15 6"></polyline></svg>
					</button>
					<div class="slide-dots-container" id="slide-dots-container">
						<!-- Dots generated dynamically -->
					</div>
					<button type="button" class="nav-control-btn next-point-btn" id="next-point-btn" title="Next Point (Spacebar or →)">
						<svg viewBox="0 0 24 24" width="18" height="18" stroke="currentColor" stroke-width="2.5" fill="none"><polyline points="9 18 15 12 9 6"></polyline></svg>
					</button>
				</div>
				<div class="footer-right">
					<button type="button" class="stage-icon-btn help-btn" id="help-btn" title="Help / Keyboard Shortcuts (?)">
						<span class="help-question-mark">?</span>
					</button>
					<button type="button" class="stage-icon-btn toggle-fullscreen-btn" title="Fullscreen Presenter Mode (F)">
						<svg viewBox="0 0 24 24" width="16" height="16" stroke="currentColor" stroke-width="2" fill="none"><path d="M8 3H5a2 2 0 0 0-2 2v3m18 0V5a2 2 0 0 0-2-2h-3m0 18h3a2 2 0 0 0 2-2v-3M3 16v3a2 2 0 0 0 2 2h3"></path></svg>
					</button>
				</div>
			</div>

			<!-- Help Modal Overlay -->
			<div class="help-modal-overlay" id="help-modal-overlay">
				<div class="help-modal-card">
					<div class="help-modal-header">
						<div class="help-modal-title">
							<span class="help-badge">?</span>
							<h3>Presenter Shortcuts & Controls</h3>
						</div>
						<button type="button" class="help-close-btn" id="help-close-btn">&times;</button>
					</div>
					<div class="help-grid">
						<div class="help-row">
							<span class="help-action">Next bullet point</span>
							<div class="help-keys">
								<kbd>Spacebar</kbd> <kbd>→</kbd> <kbd>Left-Click</kbd>
							</div>
						</div>
						<div class="help-row">
							<span class="help-action">Previous bullet point</span>
							<div class="help-keys">
								<kbd>Shift+Space</kbd> <kbd>←</kbd> <kbd>Right-Click</kbd>
							</div>
						</div>
						<div class="help-row">
							<span class="help-action">Jump to next slide</span>
							<div class="help-keys">
								<kbd>↓</kbd> <kbd>PageDown</kbd>
							</div>
						</div>
						<div class="help-row">
							<span class="help-action">Jump to previous slide</span>
							<div class="help-keys">
								<kbd>↑</kbd> <kbd>PageUp</kbd>
							</div>
						</div>
						<div class="help-row">
							<span class="help-action">Toggle Fullscreen (OBS Mode)</span>
							<div class="help-keys">
								<kbd>F</kbd>
							</div>
						</div>
						<div class="help-row">
							<span class="help-action">Toggle shortcuts help</span>
							<div class="help-keys">
								<kbd>?</kbd> or <kbd>H</kbd>
							</div>
						</div>
					</div>
					<div class="help-note">
						<strong>Viewer Links:</strong> Open <a href="/viewer/public" target="_blank" style="color: #60a5fa; font-weight: bold;">/viewer/public</a> for screen recording, or <a href="/viewer/private" target="_blank" style="color: #f87171; font-weight: bold;">/viewer/private</a> for teleprompter notes.
					</div>
				</div>
			</div>
		</div>
	</div>

	<!-- PowerPoint-Style Slide Thumbnail Rail -->
	<div class="thumbnail-preview-section">
		<div class="thumbnail-header">
			<h4>Slide Navigator (Click any slide to jump)</h4>
			<span class="thumbnail-count">8 Slides in this presentation</span>
		</div>
		<div class="thumbnail-strip" id="thumbnail-strip">
			{slides.map((s, idx) => (
				<button 
					type="button" 
					class={`thumb-card ${idx === 0 ? 'is-active' : ''}`}
					data-slide-index={idx}
					id={`thumb-btn-${idx}`}
				>
					<div class="thumb-screen">
						<div class="thumb-header">
							<span class="thumb-num">Slide {idx + 1}</span>
							<span class="thumb-badge">{s.badge}</span>
						</div>
						<div class="thumb-title">{s.title}</div>
						<div class="thumb-lines">
							<div class="thumb-line"></div>
							<div class="thumb-line short"></div>
						</div>
					</div>
				</button>
			))}
		</div>
	</div>
</div>

<!-- Slide Deck Client Script -->
<script define:vars={{ slides }}>
	(function () {
		let currentSlideIndex = 0;
		let currentPointIndex = 0;
		const totalSlides = slides.length;

		// DOM Elements
		const stageContainer = document.getElementById('slide-stage-container');
		const stage = document.getElementById('slide-stage');
		const titleEl = document.getElementById('slide-title');
		const bulletsContainer = document.getElementById('slide-bullets');
		const counterBadge = document.getElementById('slide-counter-badge');
		const topicBadge = document.getElementById('slide-topic-badge');
		const pointCounter = document.getElementById('point-counter');
		const dotsContainer = document.getElementById('slide-dots-container');
		const prevBtn = document.getElementById('prev-point-btn');
		const nextBtn = document.getElementById('next-point-btn');
		const helpBtn = document.getElementById('help-btn');
		const helpOverlay = document.getElementById('help-modal-overlay');
		const helpCloseBtn = document.getElementById('help-close-btn');
		const fullscreenBtns = document.querySelectorAll('.toggle-fullscreen-btn');

		function initDots() {
			if (!dotsContainer) return;
			dotsContainer.innerHTML = '';
			for (let i = 0; i < totalSlides; i++) {
				const dot = document.createElement('button');
				dot.type = 'button';
				dot.className = `slide-dot ${i === 0 ? 'is-active' : ''}`;
				dot.title = `Jump to slide ${i + 1}`;
				dot.addEventListener('click', (e) => {
					e.stopPropagation();
					jumpToSlide(i);
				});
				dotsContainer.appendChild(dot);
			}
		}

		function renderSlide() {
			const slide = slides[currentSlideIndex];
			if (!slide) return;

			if (counterBadge) {
				const padSlide = String(currentSlideIndex + 1).padStart(2, '0');
				const padTotal = String(totalSlides).padStart(2, '0');
				counterBadge.textContent = `SLIDE ${padSlide} / ${padTotal}`;
			}
			if (topicBadge) {
				topicBadge.textContent = slide.badge || `Topic ${currentSlideIndex + 1}`;
			}

			if (titleEl) {
				titleEl.textContent = slide.title;
			}

			if (bulletsContainer) {
				bulletsContainer.innerHTML = '';
				slide.bullets.forEach((bulletText, bIdx) => {
					const li = document.createElement('li');
					li.className = 'bullet-item';

					if (bIdx === currentPointIndex) {
						li.classList.add('is-active');
					} else if (bIdx < currentPointIndex) {
						li.classList.add('is-revealed');
					} else {
						li.classList.add('is-dimmed');
					}

					li.innerHTML = `
						<span class="bullet-marker"></span>
						<span class="bullet-text">${bulletText}</span>
					`;
					
					li.addEventListener('click', (e) => {
						e.stopPropagation();
						currentPointIndex = bIdx;
						updateBulletClasses();
						updatePointCounter();
					});

					bulletsContainer.appendChild(li);
				});
			}

			updatePointCounter();
			updateThumbnailStrip();
			updateDots();
		}

		function updateBulletClasses() {
			if (!bulletsContainer) return;
			const items = bulletsContainer.querySelectorAll('.bullet-item');
			items.forEach((item, bIdx) => {
				item.classList.remove('is-active', 'is-revealed', 'is-dimmed');
				if (bIdx === currentPointIndex) {
					item.classList.add('is-active');
				} else if (bIdx < currentPointIndex) {
					item.classList.add('is-revealed');
				} else {
					item.classList.add('is-dimmed');
				}
			});
		}

		function updatePointCounter() {
			const slide = slides[currentSlideIndex];
			if (!slide || !pointCounter) return;
			pointCounter.textContent = `Point ${currentPointIndex + 1} of ${slide.bullets.length}`;
		}

		function updateDots() {
			if (!dotsContainer) return;
			const dots = dotsContainer.querySelectorAll('.slide-dot');
			dots.forEach((dot, idx) => {
				if (idx === currentSlideIndex) {
					dot.classList.add('is-active');
				} else {
					dot.classList.remove('is-active');
				}
			});
		}

		function updateThumbnailStrip() {
			const thumbs = document.querySelectorAll('.thumb-card');
			thumbs.forEach((thumb, idx) => {
				if (idx === currentSlideIndex) {
					thumb.classList.add('is-active');
					thumb.scrollIntoView({ behavior: 'smooth', block: 'nearest', inline: 'center' });
				} else {
					thumb.classList.remove('is-active');
				}
			});
		}

		function nextStep() {
			const slide = slides[currentSlideIndex];
			if (!slide) return;

			if (currentPointIndex < slide.bullets.length - 1) {
				currentPointIndex++;
				updateBulletClasses();
				updatePointCounter();
			} else {
				if (currentSlideIndex < totalSlides - 1) {
					currentSlideIndex++;
					currentPointIndex = 0;
					renderSlide();
				}
			}
		}

		function prevStep() {
			if (currentPointIndex > 0) {
				currentPointIndex--;
				updateBulletClasses();
				updatePointCounter();
			} else {
				if (currentSlideIndex > 0) {
					currentSlideIndex--;
					const prevSlide = slides[currentSlideIndex];
					currentPointIndex = prevSlide.bullets.length - 1;
					renderSlide();
				}
			}
		}

		function nextSlide() {
			if (currentSlideIndex < totalSlides - 1) {
				currentSlideIndex++;
				currentPointIndex = 0;
				renderSlide();
			}
		}

		function prevSlide() {
			if (currentSlideIndex > 0) {
				currentSlideIndex--;
				currentPointIndex = 0;
				renderSlide();
			}
		}

		function jumpToSlide(idx) {
			if (idx >= 0 && idx < totalSlides) {
				currentSlideIndex = idx;
				currentPointIndex = 0;
				renderSlide();
			}
		}

		function toggleFullscreen() {
			const target = stageContainer || document.documentElement;
			if (!document.fullscreenElement) {
				if (target.requestFullscreen) {
					target.requestFullscreen();
				}
			} else {
				if (document.exitFullscreen) {
					document.exitFullscreen();
				}
			}
		}

		function toggleHelp(show) {
			if (!helpOverlay) return;
			if (typeof show === 'boolean') {
				helpOverlay.style.display = show ? 'flex' : 'none';
			} else {
				helpOverlay.style.display = helpOverlay.style.display === 'flex' ? 'none' : 'flex';
			}
		}

		prevBtn?.addEventListener('click', (e) => {
			e.stopPropagation();
			prevStep();
		});

		nextBtn?.addEventListener('click', (e) => {
			e.stopPropagation();
			nextStep();
		});

		helpBtn?.addEventListener('click', (e) => {
			e.stopPropagation();
			toggleHelp();
		});

		helpCloseBtn?.addEventListener('click', (e) => {
			e.stopPropagation();
			toggleHelp(false);
		});

		helpOverlay?.addEventListener('click', (e) => {
			if (e.target === helpOverlay) {
				toggleHelp(false);
			}
		});

		fullscreenBtns.forEach((btn) => {
			btn.addEventListener('click', (e) => {
				e.stopPropagation();
				toggleFullscreen();
			});
		});

		document.querySelectorAll('.thumb-card').forEach((card) => {
			card.addEventListener('click', () => {
				const idx = parseInt(card.getAttribute('data-slide-index') || '0', 10);
				jumpToSlide(idx);
			});
		});

		stage?.addEventListener('click', (e) => {
			if (e.target.closest('button') || e.target.closest('a') || e.target.closest('.help-modal-card')) return;
			nextStep();
		});

		stage?.addEventListener('contextmenu', (e) => {
			e.preventDefault();
			prevStep();
		});

		stage?.addEventListener('auxclick', (e) => {
			if (e.button === 1) {
				e.preventDefault();
				toggleHelp();
			}
		});

		window.addEventListener('keydown', (e) => {
			if (['INPUT', 'TEXTAREA'].includes(document.activeElement?.tagName || '')) return;

			if (e.code === 'Space') {
				e.preventDefault();
				if (e.shiftKey) {
					prevStep();
				} else {
					nextStep();
				}
			} else if (e.code === 'ArrowRight') {
				e.preventDefault();
				nextStep();
			} else if (e.code === 'ArrowLeft') {
				e.preventDefault();
				prevStep();
			} else if (e.code === 'ArrowDown' || e.code === 'PageDown') {
				e.preventDefault();
				nextSlide();
			} else if (e.code === 'ArrowUp' || e.code === 'PageUp') {
				e.preventDefault();
				prevSlide();
			} else if (e.code === 'KeyF') {
				e.preventDefault();
				toggleFullscreen();
			} else if (e.code === 'KeyH' || e.key === '?') {
				e.preventDefault();
				toggleHelp();
			} else if (e.code === 'Escape') {
				toggleHelp(false);
			}
		});

		document.addEventListener('fullscreenchange', () => {
			if (document.fullscreenElement) {
				stageContainer?.classList.add('is-in-fullscreen');
			} else {
				stageContainer?.classList.remove('is-in-fullscreen');
			}
		});

		initDots();
		renderSlide();
	})();
</script>

<style is:global>
	.deck-wrapper {
		margin: 2.5rem 0 3.5rem 0;
		width: 100%;
		font-family: var(--font-body, system-ui, sans-serif);
	}

	.deck-wrapper.standalone-mode {
		margin: 0;
		padding: 0;
		width: 100vw;
		height: 100vh;
		background: #07090e;
		display: flex;
		flex-direction: column;
		justify-content: center;
		align-items: center;
		overflow: hidden;
	}

	.deck-top-bar {
		display: flex;
		justify-content: space-between;
		align-items: center;
		padding: 0.85rem 1.25rem;
		background: #0f131a;
		border: 1px solid #1e2638;
		border-bottom: none;
		border-radius: 12px 12px 0 0;
	}

	.deck-title-group {
		display: flex;
		align-items: center;
		gap: 0.85rem;
	}

	.deck-live-badge {
		font-size: 0.72rem;
		font-weight: 700;
		letter-spacing: 0.08em;
		color: #ef4444;
		background: rgba(239, 68, 68, 0.12);
		border: 1px solid rgba(239, 68, 68, 0.3);
		padding: 0.2rem 0.6rem;
		border-radius: 4px;
	}

	.deck-meta {
		font-size: 0.82rem;
		color: #94a3b8;
	}

	.deck-actions {
		display: flex;
		align-items: center;
		gap: 0.6rem;
	}

	.deck-btn {
		display: inline-flex;
		align-items: center;
		gap: 0.45rem;
		font-size: 0.78rem;
		font-weight: 600;
		padding: 0.45rem 0.75rem;
		border-radius: 6px;
		cursor: pointer;
		text-decoration: none;
		transition: all 0.18s ease;
	}

	.deck-btn-outline {
		color: #cbd5e1;
		background: rgba(255, 255, 255, 0.04);
		border: 1px solid #334155;
	}

	.deck-btn-outline:hover {
		background: rgba(255, 255, 255, 0.08);
		color: #ffffff;
		border-color: #64748b;
	}

	.deck-btn-primary {
		color: #ffffff;
		background: #c8382c;
		border: 1px solid #dc2626;
	}

	.deck-btn-primary:hover {
		background: #dc2626;
		box-shadow: 0 0 12px rgba(220, 38, 38, 0.4);
	}

	.slide-stage-container {
		position: relative;
		width: 100%;
		aspect-ratio: 16 / 9;
		background: #0a0c10;
		border: 1px solid #1e2638;
		border-radius: 0 0 0 0;
		overflow: hidden;
		outline: none;
		box-shadow: 0 20px 45px -10px rgba(0, 0, 0, 0.6);
		user-select: none;
	}

	.standalone-mode .slide-stage-container {
		border: none;
		border-radius: 0;
		box-shadow: none;
		max-height: 100vh;
		max-width: 177.78vh;
	}

	.slide-stage-container:fullscreen,
	.slide-stage-container.is-in-fullscreen {
		width: 100vw !important;
		height: 100vh !important;
		max-width: none !important;
		max-height: none !important;
		border: none !important;
		border-radius: 0 !important;
	}

	.slide-stage {
		position: relative;
		width: 100%;
		height: 100%;
		padding: 2.2rem 3rem 2rem 3rem;
		display: flex;
		flex-direction: column;
		justify-content: space-between;
		background: radial-gradient(circle 900px at 50% -20%, rgba(200, 56, 44, 0.15), transparent 70%),
		            linear-gradient(180deg, #0d1118 0%, #080a0e 100%);
		cursor: pointer;
	}

	.slide-stage-header {
		display: flex;
		justify-content: space-between;
		align-items: center;
		width: 100%;
	}

	.slide-badge-box {
		display: flex;
		align-items: center;
		gap: 0.65rem;
	}

	.slide-counter-badge {
		font-family: monospace;
		font-size: 0.78rem;
		font-weight: 700;
		letter-spacing: 0.08em;
		color: #cbd5e1;
		background: rgba(255, 255, 255, 0.06);
		padding: 0.25rem 0.6rem;
		border-radius: 4px;
		border: 1px solid rgba(255, 255, 255, 0.1);
	}

	.slide-topic-badge {
		font-size: 0.75rem;
		font-weight: 600;
		letter-spacing: 0.04em;
		color: #f87171;
		background: rgba(239, 68, 68, 0.08);
		padding: 0.25rem 0.65rem;
		border-radius: 4px;
		border: 1px solid rgba(239, 68, 68, 0.2);
	}

	.slide-brand {
		display: flex;
		align-items: center;
		gap: 0.6rem;
	}

	.brand-logo-mini {
		width: 26px;
		height: 26px;
		display: flex;
		align-items: center;
		justify-content: center;
	}

	.brand-text {
		font-size: 0.88rem;
		font-weight: 700;
		letter-spacing: -0.01em;
		color: #e2e8f0;
	}

	.slide-body {
		flex: 1;
		display: flex;
		flex-direction: column;
		justify-content: center;
		padding: 0.5rem 0;
	}

	.slide-title {
		font-family: var(--font-heading, system-ui, sans-serif);
		font-size: clamp(1.65rem, 3.2vw, 2.6rem);
		font-weight: 800;
		line-height: 1.18;
		color: #ffffff;
		margin-bottom: 1.5rem;
		text-wrap: balance;
		letter-spacing: -0.02em;
	}

	.slide-bullets {
		list-style: none;
		display: flex;
		flex-direction: column;
		gap: clamp(0.85rem, 1.8vw, 1.35rem);
		padding: 0;
		margin: 0;
	}

	.bullet-item {
		display: flex;
		align-items: center;
		gap: 1.15rem;
		font-size: clamp(1.35rem, 2.3vw, 1.85rem);
		line-height: 1.35;
		padding: 0.5rem 0.85rem;
		border-radius: 8px;
		transition: all 0.22s cubic-bezier(0.16, 1, 0.3, 1);
		cursor: pointer;
	}

	.slide-stage-container:fullscreen .bullet-item,
	.slide-stage-container.is-in-fullscreen .bullet-item {
		font-size: clamp(1.65rem, 2.7vw, 2.4rem);
		gap: 1.35rem;
		padding: 0.65rem 1.15rem;
	}

	.bullet-marker {
		display: inline-block;
		width: 14px;
		height: 14px;
		border-radius: 50%;
		flex-shrink: 0;
		transition: all 0.2s ease;
	}

	.slide-stage-container:fullscreen .bullet-marker,
	.slide-stage-container.is-in-fullscreen .bullet-marker {
		width: 18px;
		height: 18px;
	}

	.bullet-text {
		color: #ffffff !important;
		flex: 1;
	}

	/* 1. LIVE (ACTIVE) POINT: Glowing Emerald Green (Dennis: green for the live one) */
	.bullet-item.is-active {
		opacity: 1 !important;
		background: rgba(34, 197, 94, 0.18) !important;
		border: 2px solid #22c55e !important;
		box-shadow: 0 0 20px rgba(34, 197, 94, 0.35), inset 0 0 10px rgba(34, 197, 94, 0.15) !important;
		transform: translateX(6px);
	}

	.bullet-item.is-active .bullet-marker {
		background: #22c55e !important;
		box-shadow: 0 0 14px #22c55e, 0 0 28px rgba(34, 197, 94, 0.7) !important;
		transform: scale(1.35);
	}

	.bullet-item.is-active .bullet-text {
		color: #ffffff !important;
		font-weight: 800 !important;
		text-shadow: 0 2px 8px rgba(0, 0, 0, 0.8);
	}

	/* 2. INACTIVE / UPCOMING POINTS: Red Dot Accent with 100% VISIBLE TEXT (Dennis: red for inactive, text visible all the time) */
	.bullet-item.is-dimmed {
		opacity: 1 !important; /* 100% visible, NEVER dark or faded */
		background: rgba(239, 68, 68, 0.06) !important;
		border: 1.5px solid rgba(239, 68, 68, 0.35) !important;
		transform: translateX(0);
	}

	.bullet-item.is-dimmed .bullet-marker {
		background: #ef4444 !important;
		box-shadow: 0 0 10px rgba(239, 68, 68, 0.7) !important;
		transform: scale(1);
	}

	.bullet-item.is-dimmed .bullet-text {
		color: #f1f5f9 !important; /* Crisp, readable light text */
		font-weight: 600 !important;
	}

	/* 3. REVEALED / PAST POINTS: Clean finished status with soft gray dot */
	.bullet-item.is-revealed {
		opacity: 0.88 !important;
		background: rgba(255, 255, 255, 0.03) !important;
		border: 1.5px solid rgba(255, 255, 255, 0.1) !important;
		transform: translateX(0);
	}

	.bullet-item.is-revealed .bullet-marker {
		background: #64748b !important;
		box-shadow: none !important;
	}

	.bullet-item.is-revealed .bullet-text {
		color: #cbd5e1 !important;
		font-weight: 600 !important;
	}

	.slide-stage-footer {
		display: flex;
		justify-content: space-between;
		align-items: center;
		width: 100%;
		padding-top: 0.75rem;
		border-top: 1px solid rgba(255, 255, 255, 0.08);
	}

	.footer-left {
		display: flex;
		align-items: center;
		gap: 0.85rem;
	}

	.point-counter {
		font-size: 0.75rem;
		font-weight: 600;
		color: #94a3b8;
	}

	.footer-hint {
		font-size: 0.75rem;
		color: #64748b;
	}

	kbd {
		background: rgba(255, 255, 255, 0.08);
		border: 1px solid rgba(255, 255, 255, 0.15);
		border-radius: 4px;
		padding: 0.15rem 0.4rem;
		font-family: monospace;
		font-size: 0.72rem;
		color: #f1f5f9;
	}

	.footer-center {
		display: flex;
		align-items: center;
		gap: 0.65rem;
	}

	.nav-control-btn {
		width: 30px;
		height: 30px;
		display: flex;
		align-items: center;
		justify-content: center;
		background: rgba(255, 255, 255, 0.05);
		border: 1px solid rgba(255, 255, 255, 0.12);
		border-radius: 50%;
		color: #cbd5e1;
		cursor: pointer;
		transition: all 0.18s ease;
	}

	.nav-control-btn:hover {
		background: rgba(255, 255, 255, 0.12);
		color: #ffffff;
		border-color: #94a3b8;
	}

	.slide-dots-container {
		display: flex;
		align-items: center;
		gap: 0.4rem;
	}

	.slide-dot {
		width: 7px;
		height: 7px;
		border-radius: 50%;
		background: rgba(255, 255, 255, 0.2);
		border: none;
		cursor: pointer;
		padding: 0;
		transition: all 0.2s ease;
	}

	.slide-dot.is-active {
		background: #ef4444;
		transform: scale(1.4);
		box-shadow: 0 0 6px #ef4444;
	}

	.footer-right {
		display: flex;
		align-items: center;
		gap: 0.5rem;
	}

	.stage-icon-btn {
		width: 30px;
		height: 30px;
		display: flex;
		align-items: center;
		justify-content: center;
		background: rgba(255, 255, 255, 0.05);
		border: 1px solid rgba(255, 255, 255, 0.12);
		border-radius: 6px;
		color: #cbd5e1;
		cursor: pointer;
		transition: all 0.18s ease;
	}

	.stage-icon-btn:hover {
		background: rgba(255, 255, 255, 0.12);
		color: #ffffff;
		border-color: #94a3b8;
	}

	.help-question-mark {
		font-size: 0.95rem;
		font-weight: 700;
	}

	.help-modal-overlay {
		position: absolute;
		top: 0;
		left: 0;
		width: 100%;
		height: 100%;
		background: rgba(5, 7, 10, 0.85);
		backdrop-filter: blur(8px);
		display: none;
		align-items: center;
		justify-content: center;
		z-index: 50;
		padding: 1.5rem;
	}

	.help-modal-card {
		background: #0f131a;
		border: 1px solid #28334a;
		border-radius: 12px;
		width: 100%;
		max-width: 520px;
		padding: 1.6rem 1.8rem;
		box-shadow: 0 25px 60px rgba(0, 0, 0, 0.8);
		cursor: default;
	}

	.help-modal-header {
		display: flex;
		justify-content: space-between;
		align-items: center;
		margin-bottom: 1.25rem;
	}

	.help-modal-title {
		display: flex;
		align-items: center;
		gap: 0.65rem;
	}

	.help-badge {
		width: 24px;
		height: 24px;
		border-radius: 50%;
		background: #c8382c;
		color: #fff;
		font-size: 0.8rem;
		font-weight: bold;
		display: flex;
		align-items: center;
		justify-content: center;
	}

	.help-modal-title h3 {
		font-size: 1.15rem;
		color: #ffffff;
		font-weight: 700;
		margin: 0;
	}

	.help-close-btn {
		background: transparent;
		border: none;
		color: #94a3b8;
		font-size: 1.5rem;
		line-height: 1;
		cursor: pointer;
		padding: 0.2rem;
	}

	.help-close-btn:hover {
		color: #ffffff;
	}

	.help-grid {
		display: flex;
		flex-direction: column;
		gap: 0.75rem;
		margin-bottom: 1.25rem;
	}

	.help-row {
		display: flex;
		justify-content: space-between;
		align-items: center;
		padding: 0.5rem 0.75rem;
		background: rgba(255, 255, 255, 0.03);
		border-radius: 6px;
	}

	.help-action {
		font-size: 0.88rem;
		color: #cbd5e1;
	}

	.help-keys {
		display: flex;
		gap: 0.35rem;
	}

	.help-note {
		font-size: 0.82rem;
		color: #94a3b8;
		background: rgba(200, 56, 44, 0.1);
		border: 1px solid rgba(200, 56, 44, 0.25);
		border-radius: 6px;
		padding: 0.65rem 0.85rem;
	}

	.thumbnail-preview-section {
		margin-top: 1rem;
		background: #0f131a;
		border: 1px solid #1e2638;
		border-radius: 0 0 12px 12px;
		padding: 1.25rem;
	}

	.standalone-mode .thumbnail-preview-section {
		display: none;
	}

	.thumbnail-header {
		display: flex;
		justify-content: space-between;
		align-items: center;
		margin-bottom: 0.9rem;
	}

	.thumbnail-header h4 {
		font-size: 0.88rem;
		font-weight: 700;
		color: #e2e8f0;
		margin: 0;
	}

	.thumbnail-count {
		font-size: 0.78rem;
		color: #64748b;
	}

	.thumbnail-strip {
		display: flex;
		gap: 0.85rem;
		overflow-x: auto;
		padding-bottom: 0.5rem;
		scrollbar-width: thin;
		scrollbar-color: #334155 #0a0c10;
	}

	.thumbnail-strip::-webkit-scrollbar {
		height: 6px;
	}

	.thumbnail-strip::-webkit-scrollbar-thumb {
		background: #334155;
		border-radius: 3px;
	}

	.thumb-card {
		flex: 0 0 160px;
		height: 94px;
		background: #07090e;
		border: 1.5px solid #1e2638;
		border-radius: 6px;
		padding: 0.5rem;
		cursor: pointer;
		text-align: left;
		transition: all 0.18s ease;
		display: flex;
		flex-direction: column;
		justify-content: space-between;
	}

	.thumb-card:hover {
		border-color: #64748b;
		transform: translateY(-2px);
	}

	.thumb-card.is-active {
		border-color: #ef4444;
		box-shadow: 0 0 10px rgba(239, 68, 68, 0.4);
	}

	.thumb-screen {
		display: flex;
		flex-direction: column;
		justify-content: space-between;
		height: 100%;
	}

	.thumb-header {
		display: flex;
		justify-content: space-between;
		align-items: center;
	}

	.thumb-num {
		font-family: monospace;
		font-size: 0.65rem;
		color: #94a3b8;
		font-weight: 600;
	}

	.thumb-badge {
		font-size: 0.58rem;
		color: #f87171;
		background: rgba(239, 68, 68, 0.1);
		padding: 0.1rem 0.35rem;
		border-radius: 3px;
	}

	.thumb-title {
		font-size: 0.72rem;
		font-weight: 600;
		color: #e2e8f0;
		line-height: 1.25;
		margin: 0.3rem 0;
		display: -webkit-box;
		-webkit-line-clamp: 2;
		-webkit-box-orient: vertical;
		overflow: hidden;
	}

	.thumb-lines {
		display: flex;
		flex-direction: column;
		gap: 3px;
	}

	.thumb-line {
		height: 2px;
		background: rgba(255, 255, 255, 0.1);
		border-radius: 1px;
		width: 90%;
	}

	.thumb-line.short {
		width: 55%;
	}

	@media (max-width: 768px) {
		.slide-stage {
			padding: 1.2rem 1.4rem 1rem 1.4rem;
		}
		.footer-hint {
			display: none;
		}
		.deck-top-bar {
			flex-direction: column;
			gap: 0.6rem;
			align-items: flex-start;
		}
		.deck-actions {
			width: 100%;
			justify-content: space-between;
		}
		.thumb-card {
			flex: 0 0 130px;
			height: 80px;
		}
	}
</style>
